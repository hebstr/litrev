[project]
name = "literature-review"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "module-inspector>=0.2.0",
    "pip>=26.0.1",
    "requests",
]

[project.optional-dependencies]
test = ["pytest"]
